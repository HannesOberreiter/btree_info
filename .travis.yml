language: node_js
node_js:
  - '8'

cache:
  yarn: true
  directories:
    - node_modules

branches:
  only:
    - master

install:
  - yarn install
  - yarn generate

script:
  - echo "Skipping tests"

env:
after_success:
- find /dist/ -exec curl -T {} ftp://public_html/www_btree_at/deploy/ --user ${user}:${password} \;